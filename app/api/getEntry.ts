// import { getEntryById } from "@/lib/storage";
// import { get, set } from 'idb-keyval';
// // pages/api/getEntry.ts
// export default function handler(req, res) {
//     const { id } = req.query;
  
//     // Simulate fetching data based on the id from a database or file system
//     const entryData = getEntryById(id); // Replace with actual logic
  
//     if (entryData) {
//       res.status(200).json(entryData);
//     } else {
//       res.status(404).json({ message: 'Entry not found' });
//     }
//   }
  